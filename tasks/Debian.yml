---
- name: Install base packages
  apt:
    name: "{{packages}}"
    state: present
    update_cache: yes
  vars:
    packages:
    - git
    - tig
    - mc
    - htop
    - wget
    - tmux
    - vim
    - unzip
    - lnav
    - python-pip
    - pydf
    - zsh
