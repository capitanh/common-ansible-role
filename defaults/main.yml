---
###############################################################################
# General Config
admin_user:           admin                     # Administration user for all hosts
admin_password:       admin                     # Admin user password
procs_version:        v0.11.4                   # procs (ps alternative) version
gdu_version:          v4.11.1                   # gdu (du alternative) version
fzf_version:          0.27.0                    # fzf (fuzzy finder) version
navi_version:         v2.16.0                   # navi (interactive cheatsheets) version
tools:
  - procs:  "https://github.com/dalance/procs/releases/download/{{procs_version}}/procs-{{procs_version}}-x86_64-lnx.zip"
  - gdu:    "https://github.com/dundee/gdu/releases/download/{{gdu_version}}/gdu_linux_amd64.tgz"
  - fzf:    "https://github.com/junegunn/fzf/releases/download/{{fzf_version}}/fzf-{{fzf_version}}-linux_amd64.tar.gz"
  - navi:   "https://github.com/denisidoro/navi/releases/download/{{navi_version}}/navi-{{navi_version}}-x86_64-unknown-linux-musl.tar.gz"
